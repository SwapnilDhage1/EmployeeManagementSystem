# EmployeeManagementSystem
A full-stack Employee Management System built with ASP.NET (C#) as the backend, ReactJS with Modern UI as the frontend, and SQL Server as the database.   This project showcases CRUD operations with validations, dynamic UI behavior, and integration with charting and reporting features.
